# 🚀 Project-0 (web-MVP) - Установка и Запуск

## ✅ Что исправлено:

- ❌ Убрана проблема с `bcrypt`
- ✅ Используется безопасный SHA256 с солью для хеширования паролей
- ✅ Все зависимости упрощены

## 📦 Установка зависимостей:

```bash
pip install fastapi uvicorn httpx pydantic python-multipart python-jose[cryptography]
```

Или используйте файл requirements.txt:

```bash
pip install -r requirements.txt
```

## 🚀 Запуск приложения:

```bash
python3 webai.py
```

## 🌐 Доступ:

- **Главная страница**: http://localhost:8000
- **Админ панель**: http://localhost:8000/admin

## 👤 Админ доступ:

- **Логин**: `Yernur@`
- **Пароль**: `ernur140707`

## 💳 Оплата кредитов:

- **Kaspi**: +7-708-202-1308
- **Получатель**: Канат Е.

## ⚙️ Требования:

1. **Python 3.8+**
2. **Ollama** должен быть запущен:
   ```bash
   ollama serve
   ```
3. **Модель GLM** должна быть загружена:
   ```bash
   ollama pull glm-4.6:cloud
   ```

## 📊 Функции:

✅ Регистрация и вход пользователей
✅ 3 бесплатных кредита при регистрации
✅ Система кредитов (1 кредит = 1 AI запрос)
✅ Запросы на пополнение кредитов
✅ Админ панель с аналитикой
✅ Переключатель светлой/темной темы
✅ Генерация MVP с помощью AI
✅ Скачивание проектов в ZIP

## 🐛 Исправление ошибок:

Если возникают ошибки с библиотеками, попробуйте:

```bash
# Обновить pip
pip install --upgrade pip

# Установить зависимости заново
pip install --force-reinstall fastapi uvicorn httpx pydantic python-multipart python-jose[cryptography]
```

## 📝 База данных:

Все данные сохраняются в `project0.db` (SQLite)
- Пользователи
- Запросы на кредиты
- Админы

---

**Готово! Приложение готово к использованию! 🎉**
