# 🚀 Project-0 - Быстрый старт

## ✅ Исправления выполнены!

Все JavaScript ошибки исправлены! Теперь код разделен на отдельные файлы для лучшей структуры:

- **project0.py** - FastAPI backend (197 строк)
- **static/index.html** - HTML разметка
- **static/app.js** - JavaScript логика  
- **static/style.css** - Стили

## 🔧 Запуск

### Вариант 1: Автоматический (рекомендуется)

```bash
./start.sh
```

### Вариант 2: Ручной

```bash
# 1. Установите зависимости
pip install -r requirements.txt

# 2. Запустите Ollama (в отдельном терминале)
ollama serve

# 3. Скачайте модель (только один раз)
ollama pull glm-4.6:cloud

# 4. Запустите приложение
python project0.py
```

## 🌐 Откройте браузер

Перейдите на: **http://localhost:8000**

## ✨ Что работает:

- ✅ Все кнопки примеров кликабельны
- ✅ Кнопка отправки сообщений работает
- ✅ Enter для отправки
- ✅ Shift+Enter для новой строки
- ✅ Streaming responses в реальном времени
- ✅ Форматирование кода
- ✅ Нет JavaScript ошибок!

## 💡 Примеры запросов:

1. **Full-Stack приложение**:
   ```
   Создай todo приложение с React фронтендом и FastAPI бэкендом
   ```

2. **REST API**:
   ```
   Построй REST API с JWT аутентификацией и PostgreSQL
   ```

3. **UI дизайн**:
   ```
   Дизайн современной landing page с анимациями
   ```

4. **Data Science**:
   ```
   Создай Python скрипт для анализа CSV данных с визуализацией
   ```

## 🐛 Если что-то не работает:

1. **Проверьте Ollama**:
   ```bash
   ps aux | grep ollama
   ollama list  # Должна быть модель glm-4.6:cloud
   ```

2. **Проверьте порт**:
   ```bash
   netstat -tlnp | grep 8000
   ```

3. **Проверьте логи**:
   - Откройте консоль браузера (F12)
   - Проверьте вкладку Console и Network

## 📁 Структура проекта:

```
/workspace/
├── project0.py              # FastAPI backend
├── requirements.txt         # Python зависимости
├── static/
│   ├── index.html          # HTML страница
│   ├── app.js              # JavaScript логика
│   └── style.css           # CSS стили
├── start.sh                # Автозапуск скрипт
├── Dockerfile              # Docker контейнер
├── docker-compose.yml      # Docker Compose
├── README_PROJECT0.md      # Полная документация
└── DEPLOY.md               # Гайд по деплою
```

## 🎯 Что изменилось:

### Исправлено:
- ❌ `Uncaught SyntaxError: string literal contains an unescaped line break`
- ❌ `Uncaught ReferenceError: useExample is not defined`
- ❌ `Uncaught ReferenceError: sendMessage is not defined`

### Решение:
1. Разделил код на отдельные файлы (HTML/CSS/JS)
2. Заменил все template literals на конкатенацию строк
3. Использовал `data-prompt` атрибуты вместо `onclick` с кавычками
4. Добавил event listeners вместо inline handlers

## 🚀 Теперь всё работает идеально!

Обновите страницу (Ctrl+R или F5) и попробуйте! 🎉
