# 🚀 Быстрый старт Project-0

## Исправленные проблемы ✅

1. **JWT удален** - теперь используются простые сессии через cookies
2. **Скролл работает** - лендинг полностью прокручивается
3. **Пароли работают** - только bcrypt, без двойного шифрования
4. **Лендинг сначала** - модал появляется только при клике "Try It Now"

## Запуск за 3 шага

### 1. Установите зависимости
```bash
pip install -r requirements.txt
```

### 2. Запустите Ollama
```bash
ollama serve
```

### 3. Запустите платформу
```bash
python app.py
```

## Откройте браузер

```
http://localhost:8000
```

## Что увидите

1. **Красивый лендинг** со скроллом
2. **Кнопка "Try It Now"** по центру
3. **При клике** → модал регистрации/входа
4. **После входа** → чат-бот для генерации MVP

## Как работает авторизация

- **Регистрация**: username + email + любой пароль
- **Вход**: email + пароль
- **Сохранение**: через HTTP-only cookies (безопасно)
- **Шифрование**: только bcrypt для паролей
- **Без JWT**: простые сессии

## Проверка

1. Откройте http://localhost:8000
2. Прокрутите страницу вниз (должен работать скролл)
3. Нажмите "Try It Now"
4. Зарегистрируйтесь
5. Начните генерировать MVP!

## Если проблемы

```bash
# Удалите старую БД
rm -f project0_saas.db

# Перезапустите
python app.py
```

---

**Все работает! Никаких JWT, простое сохранение с шифрованием!** 🎉
